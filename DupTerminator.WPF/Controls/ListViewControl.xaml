<UserControl x:Class="DupTerminator.WPF.Controls.ListViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DupTerminator.WPF.Controls"
             xmlns:converters="clr-namespace:DupTerminator.WPF.Converters"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             x:Name="Root"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <CollectionViewSource x:Key="GroupedFiles" Source="{Binding ElementName=Root, Path=ExtendedFileInfos}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="CheckSum" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>

        <!-- Zebra Coloring Converter -->
        <converters:AlternationConverter x:Key="AlternationConverter" />

        <!-- Group Header Style -->
        <Style x:Key="GroupHeaderStyle" TargetType="{x:Type GroupItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GroupItem}">
                        <StackPanel>
                            <Border Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                                    Padding="5" Margin="0,5,0,0">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                            </Border>
                            <ItemsPresenter />
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListView Item Style -->
        <Style TargetType="{x:Type ListViewItem}">
            <Setter Property="Background" 
                    Value="{Binding RelativeSource={RelativeSource Self}, 
                    Path=(ItemsControl.AlternationIndex), 
                    Converter={StaticResource AlternationConverter}}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ListView ItemsSource="{Binding Source={StaticResource GroupedFiles}}">
            <!-- Define your ListView columns (e.g., using GridView) here -->
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Category}" />
                </GridView>
            </ListView.View>

            <!-- Define the GroupStyle -->
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="DarkBlue" Margin="2,0,0,0" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
        
        <ListView x:Name="FilesListView" 
                  ItemsSource="{Binding Source={StaticResource GroupedFiles}}"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.ScrollUnit="Item"
                  AlternationCount="2">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.GroupStyle>
                <GroupStyle ContainerStyle="{StaticResource GroupHeaderStyle}"/>
            </ListView.GroupStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200">
                        <GridViewColumn.HeaderTemplate>
                            <DataTemplate>
                                <GridViewColumnHeader Content="Name" Click="ColumnHeader_Click"/>
                            </DataTemplate>
                        </GridViewColumn.HeaderTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Size" DisplayMemberBinding="{Binding Size, StringFormat='{}{0:N0}'}" Width="100">
                        <GridViewColumn.HeaderTemplate>
                            <DataTemplate>
                                <GridViewColumnHeader Content="Size" Click="ColumnHeader_Click"/>
                            </DataTemplate>
                        </GridViewColumn.HeaderTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Path" DisplayMemberBinding="{Binding Path}" Width="300">
                        <GridViewColumn.HeaderTemplate>
                            <DataTemplate>
                                <GridViewColumnHeader Content="Path" Click="ColumnHeader_Click"/>
                            </DataTemplate>
                        </GridViewColumn.HeaderTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Extension" DisplayMemberBinding="{Binding Extension}" Width="80">
                        <GridViewColumn.HeaderTemplate>
                            <DataTemplate>
                                <GridViewColumnHeader Content="Extension" Click="ColumnHeader_Click"/>
                            </DataTemplate>
                        </GridViewColumn.HeaderTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Modified" DisplayMemberBinding="{Binding LastWriteTime, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="150">
                        <GridViewColumn.HeaderTemplate>
                            <DataTemplate>
                                <GridViewColumnHeader Content="Modified" Click="ColumnHeader_Click"/>
                            </DataTemplate>
                        </GridViewColumn.HeaderTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>