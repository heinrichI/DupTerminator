<UserControl x:Class="DupTerminator.WPF.Controls.ImageGroupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:convertors="clr-namespace:DupTerminator.WPF.Converters"
             xmlns:local="clr-namespace:DupTerminator.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <convertors:FileSizeConverter x:Key="fileSizeConverter"/>
        <BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter" />
        <!-- <Style x:Key="ImageItemStyle" TargetType="ContentControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" Margin="2" Padding="4">
                            <StackPanel>
                                <Image Source="{Binding Thumbnail}" 
                                       Width="200" Height="200"
                                       Stretch="Uniform"
                                       ToolTip="{Binding FilePath}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="MouseDoubleClick">
                                            <i:InvokeCommandAction Command="{Binding ViewFullSizeCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Image>

                                <TextBlock Text="{Binding FileName}" 
                                           TextWrapping="Wrap"
                                           MaxWidth="200"
                                           TextAlignment="Center"
                                           Margin="0,4,0,0"
                                           ToolTip="{Binding FilePath}"/>

                                <ProgressBar Height="4" Margin="2" 
                                             IsIndeterminate="True"
                                             Visibility="{Binding IsLoading, Converter={StaticResource booleanToVisibilityConverter}}"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>-->
    </UserControl.Resources>

    <ListBox ItemsSource="{Binding ImageGroups}" 
         ScrollViewer.VerticalScrollBarVisibility="Auto"
         VirtualizingPanel.IsVirtualizing="True"
         VirtualizingPanel.VirtualizationMode="Recycling">
        <ListBox.ItemTemplate>
            <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="0,0,0,1" Margin="0,0,0,10" Padding="0,0,0,10"
                        Focusable="True" >

                        <StackPanel>
                            <!--<TextBlock Text="{Binding DuplicateGroup.Name}" 
                                       FontWeight="Bold" Margin="0,0,0,10"/>-->

                            <ItemsControl ItemsSource="{Binding Images}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>                                
                                

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="LightGray" BorderThickness="1" Margin="2" Padding="4"
                                              Focusable="True" >
                                            <StackPanel>
                                                <Image Source="{Binding Thumbnail}" 
                                                       Width="200" Height="200"
                                                       Stretch="Uniform"
                                                        Focusable="True"
                                                       ToolTip="{Binding FilePath}">
                                                    <i:Interaction.Triggers>
                                                        <i:EventTrigger EventName="MouseLeftButtonDown">
                                                            <i:InvokeCommandAction Command="{Binding ViewFullSizeCommand}"/>
                                                        </i:EventTrigger>
                                                    </i:Interaction.Triggers>
                                                </Image>

                                            <TextBox Text="{Binding FileName, Mode=OneWay}" 
                                                     IsReadOnly="True"
                                                   TextWrapping="Wrap"
                                                   MaxWidth="200"
                                                   TextAlignment="Center"
                                                   Margin="0,4,0,0"
                                                   ToolTip="{Binding FilePath}">
                                                    <i:Interaction.Triggers>
                                                            <i:KeyTrigger Key="F2">
                                                                <i:InvokeCommandAction Command="{Binding RenameCommand}"/>
                                                            </i:KeyTrigger>
                                                    </i:Interaction.Triggers>
                                                </TextBox>
                                              
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Border BorderBrush="Black" 
                                                        BorderThickness="1" 
                                                        CornerRadius="4" 
                                                        Padding="4" 
                                                        Background="White">
                                                    <TextBlock Text="{Binding Query, Mode=OneWay}" Margin="0,0,5,0" />
                                                </Border>
                                                <Border BorderBrush="Black" 
                                                        BorderThickness="1" 
                                                        CornerRadius="4" 
                                                        Padding="4" 
                                                        Background="White">
                                                    <TextBlock Text="{Binding Dimensions, Mode=OneWay}" Margin="0,0,5,0"/>
                                                </Border>
                                                <Border BorderBrush="Black" 
                                                        BorderThickness="1" 
                                                        CornerRadius="4" 
                                                        Padding="4" 
                                                        Background="White">
                                                    <TextBlock Text="{Binding Size, Mode=OneWay, Converter={StaticResource fileSizeConverter}}" 
                                                           ToolTip="FileSize" />
                                                </Border>
                                            </StackPanel>

                                                <ProgressBar Height="4" Margin="2" 
                                                     IsIndeterminate="True"
                                                     Visibility="{Binding IsLoading, Converter={StaticResource booleanToVisibilityConverter}}"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>                                
                                
                                
                                <!--<ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="ContentTemplate">
                                            <Setter.Value>
                                                <DataTemplate>
                                                    <ContentControl Style="{StaticResource ImageItemStyle}" 
                                                                   Content="{Binding}">
                                                        <i:Interaction.Triggers>
                                                            <i:KeyTrigger Key="F2">
                                                                <i:InvokeCommandAction Command="{Binding RenameCommand}"/>
                                                            </i:KeyTrigger>
                                                        </i:Interaction.Triggers>
                                                    </ContentControl>
                                                </DataTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ItemsControl.ItemContainerStyle>-->
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
